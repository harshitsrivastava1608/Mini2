<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mini2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Organizers.AddOrganizers" >

        </activity>
        <activity android:name=".Organizers.UpdateOrganizers">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginPage" />
        <activity android:name=".UploadPdf" />
        <activity android:name=".UploadImage" >

        </activity>
        <activity android:name=".TasksGen" />
        <activity android:name=".check4" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>